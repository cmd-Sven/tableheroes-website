/** Typ für den zufälligen Lore-Teaser im Dashboard (nicht "use server"). */
export type LoreSnippet = {
  id: string;
  name: string;
  teaser: string;
  campaignId: string;
  campaignName: string;
};
