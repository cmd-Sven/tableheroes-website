"use client";

/**
 * Phase 3 Stub — Fog of War / SL-Sichtlinien.
 * @see src/lib/session/battlemap-types.ts (BattlemapFogState)
 * @see BATTLEMAP.md
 */
type Props = {
  /** Fog-Maske pro Zelle — noch nicht angebunden */
  fogState?: null;
  isGm?: boolean;
  mapWidth: number;
  mapHeight: number;
};

export function BattlemapFogLayer(_props: Props) {
  return null;
}
